# Discussions

技术讨论记录目录。记录尚未进入执行阶段的架构讨论、方案评估和技术探索。

> 与 `decisions/` (ADR) 的区别：ADR 记录已确定的技术决策；discussions 记录探索阶段的讨论，可能演变为 ADR，也可能被否决。

**状态语义**（影响可信度，AI/读者据此判断该不该信赖）：
- **调研记录**：对上游/现状的**事实性调研**（如 OpenCode 内置工具、Permission 机制、Tauri 现状）。结论可作为**权威参考**信赖，不是"待定提案"。
- **讨论中**：尚未定论的**方案提案/评估**，可能被采纳或否决，不应当作既定事实引用。

## Index

| 文件 | 主题 | 日期 | 状态 |
|------|------|------|------|
| [049-inline-markdown-image-broken-thumbnail.md](./049-inline-markdown-image-broken-thumbnail.md) | 「画个章鱼/虎鲸」回复里内联图片渲染成破图 `❓` — 根因**真实数据+代码实测双证**：qwen 纯文本不能出图 → 把 SVG/PNG 写进工作区文件、正文用 markdown `![alt](本地路径)` 引用（会话 DB 原文 `![章鱼](/Users/.../octopus.svg)` 绝对 / `![帅气虎鲸](orca_preview.png)` 相对）；而 `message-parts.tsx` 的 `MARKDOWN_COMPONENTS` **无 `img` 覆盖** → react-markdown（v9.1.0 无 rehype-raw）直接吐 `<img src=本地路径>`，WebView origin（Vite `localhost:1420`/`tauri://`）解析成 `http://localhost:1420/...` → **404 破图**；全仓无 `convertFileSrc`、未配 `assetProtocol`。**第二层新验证（定方案）**：app 已有 `getFileContent` 通道（vendor `File.read` `file/index.ts:513` `path.join(Instance.directory,file)`+`containsPath`）**只认 workspace 相对路径**——活端点实测：带 `x-opencode-directory` header + 相对 `octopus.svg`→`image/svg+xml` base64 ✅；**绝对路径被 `path.join` 拼坏→空** ❌。与产物无关（产物面板走另一条相对路径+同通道本身没坏，不碰扫描/归属，与 044 无关，确认用户判断）。**方案**=A（本地图经 `getFileContent→data:URI`）+ 协议分派（远程/`data:` 直出、本地转 URI）+ **绝对→相对**（`toWorkspaceRelative` 剥前缀，crux）+ workspace 限定（server `containsPath` 强制）+ C 兜底 chip（越界/失败/流式占位不显破图）+ module 级缓存（PNG 645KB base64 免重拉）+ 点击放大复用现有 `ArtifactPreview`（零新 UI）+ Context 透传（`about-legal` 无 workspaceDir 惰性）+ rich-output 加 1 行相对路径引导（软手段）。否决 Option B（asset protocol，e2e/web 失效、CSP/scope 移动件多）。改动中偏小、局部化聊天渲染层；风险低到中（流式/大图性能已覆盖，安全面不扩大）。详见 ADR-065 | 2026-07-22 | ✅ 根因已实证 · 已实现（desktop 712 / rich-output 15）· 真机观感待用户 |
| [048-default-agent-prompt-richness.md](./048-default-agent-prompt-richness.md) | 默认会话回复过于简短 → 系统提示富文本化（`system.transform` 保留基座 + 追加） — 根因**代码实测**：默认单 agent 会话未注入任何自定义主提示（本仓无 `build_agent_identity`/`brand.json`），qwen 走 `system.ts:33` fallback 吃 OpenCode 面向终端 CLI 的 `default.txt`，其第 17/19/94 行字面强制 `fewer than 4 lines`/`One word answers are best`/`minimize output tokens`（Claude Code 血统、为 monospace 终端设计），与我方桌面富 Markdown 产品 UX 不匹配 → 故默认平铺、需追加「展开回复下」才丰富（正命中 `unless user asks for detail` 豁免口子）。三变量拆解：提示=主因可控 · 模型 plus↔max=次因本次不动 · 渲染非瓶颈（`message-parts.tsx:65` 已启用 remark-gfm，富文本渲染得出只是模型不产出）。**注入点历经一次修订**：初版 `OPENCODE_CONFIG_CONTENT` env + `agent.build.prompt` 会**整体替换** `provider()`（`llm.ts:232` 无条件），而 `provider()` 按模型分流（`system.ts:20-33`：Claude→anthropic.txt/GPT→gpt·beast/Gemini→gemini/Kimi→kimi/其余→default），本 app 是 BYOK 多 provider（ADR-039/042）→ env 会把非 qwen 模型专属调优一并丢弃 = **降级** → 否决。定版 **Option D：插件 `experimental.chat.system.transform`**（我们已在用，`tool-disclosure.ts:215`，属已维护的 vendor patch）——**保留每模型基座**、仅对 default.txt 路径追加 `<verbosity_override>` 软化极简子句 + 追加 5 段品牌指令（语言一致性/身份/输出格式化自适应/`<task_execution>` 压编码絮叨/敏感信息防护），排除 present_files。作用域=仅 `build` 普通会话，plan/title/compaction/**Team·ACP 均不受影响**。关键坑=身份隐藏是**软护栏**（`system.ts:40` environment 始终注入真实 model/provider ID，追加段改不掉它，已接受）。副作用表 + kill switch（`experimental.rich_output`）见正文 §五。决策：全做 · 软护栏 · **Option D 注入** · UltraWork · 模型不变 | 2026-07-21 | 🟡 方案已定稿（第二轮审查修订注入点）· 未动代码 |
| [001-mobile-relay.md](./001-mobile-relay.md) | 移动端与桌面端通信 — Relay Server 方案 | 2026-03-18 | 讨论中 |
| 002-commercialization.md（本地文档，刻意不入 git——见 .gitignore） | 商业化策略 — 分发渠道 / 定价模式 / 待补功能评估 | 2026-03-22 | 讨论中 |
| [003-sidecar-sharing.md](./003-sidecar-sharing.md) | OpenCode Sidecar 能力共享 — 多进程复用方案 | 2026-05-13 | 讨论中 |
| [004-opencode-multi-agent.md](./004-opencode-multi-agent.md) | OpenCode 多 Agent 机制调研 — 默认/自定义/子 Agent 委派 | 2026-06-03 | 调研记录 |
| [005-permission-question-dock.md](./005-permission-question-dock.md) | Permission Dock 与 Question Dock 机制调研 — 挂起式权限/提问端到端实现 | 2026-06-04 | 调研记录 |
| [006-custom-llm-provider.md](./006-custom-llm-provider.md) | 自定义 LLM Provider 机制调研 — 兼容 OpenAI/Anthropic 协议的自带 Key + Base URL 接入 | 2026-06-04 | 调研记录 |
| [007-opencode-builtin-tools.md](./007-opencode-builtin-tools.md) | OpenCode 内置工具全景调研 — 功能/使用场景/实现路径/启用条件 | 2026-06-04 | 调研记录 |
| [008-opencode-builtin-agents-orchestration.md](./008-opencode-builtin-agents-orchestration.md) | OpenCode 内置 Agent 全景、相互调用机制与 runLoop 引擎 — 谁能调用谁/谁来调度/怎么驱动 | 2026-06-04 | 调研记录 |
| [009-tauri-vs-electron.md](./009-tauri-vs-electron.md) | 桌面端框架调研 — Tauri 现状、Electron 对比、能力边界与迁移代价评估 | 2026-06-04 | 调研记录 |
| [010-ai-dev-doc-quality.md](./010-ai-dev-doc-quality.md) | AI 驱动开发的文档质量保障体系 — 现状诊断与优化方案 | 2026-06-06 | 已落地 |
| [011-architecture-comparison.md](./011-architecture-comparison.md) | Ultrawork 架构横向对标 — openclaw / hermes-agent / opencode desktop（多维对比 + 优劣 + 建议） | 2026-06-08 | 调研记录 + 讨论中 |
| [012-p1-execution-plan.md](./012-p1-execution-plan.md) | P1 可执行方案 — IM 流式重构 / 持久记忆注入 / 多 Agent UI 暴露（MVP+Phase2 拆解） | 2026-06-08 | 讨论中 |
| [013-agent-os-acp-multi-backend.md](./013-agent-os-acp-multi-backend.md) | Ultrawork 作为统一交互层 / Agent OS — 经 ACP 调度多 agent 后端（opencode/claude code/qoder…）可行性与路径 | 2026-06-08 | 讨论中 |
| [014-stage1-acp-normalization-plan.md](./014-stage1-acp-normalization-plan.md) | 阶段1 可执行方案 — ACP 单 agent 异构归一化（事件桥/权限/能力/进程，含 re-baseline 与映射对照表） | 2026-06-08 | 已正式化为 ADR-027 实现章节 |
| [015-backend-taxonomy-non-acp.md](./015-backend-taxonomy-non-acp.md) | Backend 接入分类法 —「支持 ACP 非二元」与非-ACP/HTTP 后端（openclaw 薄桥 vs hermes 原生，源级实测 + 传输族×adapter 两轴 + 选型决策树） | 2026-06-09 | 调研记录 + 讨论中 |
| [016-aionui-multi-agent-competitor.md](./016-aionui-multi-agent-competitor.md) | AionUi 多 agent 调研 — 直接竞品（Electron 多-agent 桌面，20+ agent + Team Mode 编排）+ 档1/档2 实现参考 + D-8/ADR-031 现网验证（NON_ACP_BACKENDS 佐证） | 2026-06-10 | 调研记录 + 讨论中 |
| [017-team-page-agent-driven-orchestration.md](./017-team-page-agent-driven-orchestration.md) | Team 页 — agent 驱动编排的独立 surface（Leader 会话 + 委派默认化 + 普通会话物理隔离；阶段3 第三批形态提案，替代「编排模式」开关） | 2026-06-12 | ✅ 已落地（阶段3 第三批） |
| [018-unified-orchestration-ux.md](./018-unified-orchestration-ux.md) | 编排 UX 统一与视觉升级 — 模式=任务出生属性（Home 统一入口 + Team 会话进侧栏 + Session 页合流；视觉参考 AionUi，A 拍板后 B 跟进） | 2026-06-12 | ✅ 已实施（GUI 走查 10/10） |
| [019-pipeline-surface-form.md](./019-pipeline-surface-form.md) | 流水线 UI 收纳与形态 — 编排 surface 的「另一半」（伞名「自动化」+页内「流水线·Fan-out」/ footer 精简：去渠道+WiFi、连接状态迁 AgentSelector chip 按所选后端着色 / agent-avatar 视觉对齐 / 页内自我说明；功能零改动，承接 018 Q2 + 用户 footer 反馈） | 2026-06-13 | ✅ 已拍板待开工 |
| [020-vendor-bump-perf-regression.md](./020-vendor-bump-perf-regression.md) | vendor/opencode v1.15.13 性能回退分析 — 基于 v1.15.13 源码独立核验 fork 项目分析报告（坐实 health/snapshot 两主诉 + 修正 4 条夸大 + 新发现 provider/transform 正则等 3 处）+ 9 类系统性解决策略 + P0/P1/P2 ROI 路线图；本项目 bump 追赶社区前的雷区清单与解法手册，与 vendor-opencode-bump-survey 配套 | 2026-06-16 | 调研记录 + 讨论中 |
| [021-bun-avx2-baseline-sidecar.md](./021-bun-avx2-baseline-sidecar.md) | Bun 无 AVX2 CPU 崩溃 与 sidecar baseline 变体方案 — oven-sh/bun#30613（≥1.3.8 modern 变体在缺 AVX2 的 x64 上启动即崩）核验对本项目成立（构建链路只产 modern 从不产 baseline）+ 为何 `bun --compile` 把运行时打进二进制 + 三候选方案（全员 baseline / 双变体 CPU 探测 / 单独渠道）+ `--baseline` 性能/体积/构建/Tauri externalBin 负面作用清单 + 实施步骤草案 | 2026-06-24 | 调研记录 + 讨论中 |
| [022-session-switch-back-false-completion.md](./022-session-switch-back-false-completion.md) | 会话切走再切回正在运行的回合被误渲染成"已完成" — 根因（`sessionActive` 是本地 state，切换重挂清零且不从后端真相水合 → `isTurnStreaming` 兜底门控失效）多角度交叉确认（同时打坏停止按钮/权限轮询/产物扫描）+ 关键架构不对称（delegate run-state 服务端可快照查询、单会话忙碌态没有：opencode 只流 session.status、ACP 啥也没有）+ 方案 B(全局 session.status 真相)/D(ACP sidecar running 端点) 完备性评估（单 agent vs Team 同构，Leader 后端决定走 B 还是 B+D，delegate 状态充分非必要的交叉校验） | 2026-06-24 | 调研记录 + 讨论中 |
| [024-plan-progress-vs-tool-call-log.md](./024-plan-progress-vs-tool-call-log.md) | 右侧栏「计划执行进度」：工具调用日志 vs 任务规划步骤 — 现状=tool-call 流水挂了规划的名（`ProgressPanel`/`extractToolSteps` 平铺所有 assistant tool parts）；主流（Claude Code TodoWrite 整表替换→Task tools 增量 / Cursor Plan Mode / Codex / Cline）都做任务规划清单非工具流水；上游盘点=OpenCode v1.3.13 已内置完整 todo 子系统（`todowrite` 工具 + SQLite 按 session 持久化 + `todo.updated` SSE + `GET /session/{id}/todo` + `metadata.todos` 冗余 + EAGER 永不折叠）但前端零消费；Q1 推荐方案 B（规划为主 + 工具流水降级为活动兜底简单任务，A 纯替换/C 嵌套活动为备选）；Q2 完整性&对齐靠「后端真相+整表语义」（REST 水合解决切回丢失〔同 022〕+ SSE 订阅 + metadata 兜底=三路一真相；整表替换天然防漏步/幽灵步；模型忘标完成→回合结束判定降级展示不捏造）；已拍板：Q1=方案 B（规划主区+工具流水降级活动区）· Q2=连带 ACP plan 归一（ACP 现把 plan 在 `turn-shaper.onPlan` 压成 reasoning 文本丢了结构，需改回结构化；ACP 无 REST→靠历史 plan part 水合；connector 两后端归一为统一 `plan.updated`+`getPlan`）· Q3=Team 仅显当前/主 agent 会话规划不聚合子会话；下一步正式化 ADR 按 Phase 1→4 开工 | 2026-06-29 | ✅ 已落地（ADR-038） |
| [025-builtin-ppt-master-skill.md](./025-builtin-ppt-master-skill.md) | 内置 PPT 生成技能 — hugohe3/ppt-master（35.9k★/MIT/v2.12.0）调研与打包方案：已是标准 skill 形态，与 opencode skill 机制/ADR-032 管线逐项验证兼容、可零改造打包；体积 97M→裁 `ai-image-comparison`(43M 纯说明图)→gzip 实测 **6.6MB** → 推荐**路线 A 内置进安装包**（决定因素=用户网络环境不可控，GitHub 不可达则 curated 安装路线不成立）；混合模式（内置保底+curated 自助更新）可行但有**同名 skill 发现竞态**（`skill/index.ts` 同扫描 unbounded 并发 add，后写者赢不确定）→ 解法=Rust `ensure_builtin_skills` prune/restore「用户版确定性遮蔽内置版」，列阶段 2；Python 硬前置 → 依赖徽标平台化 + handoff 到 AI 对话引导安装（清华 pip 镜像）；固有代价=单 deck 几十万 token（上游逐页手写 SVG 刻意设计） | 2026-07-02 | ✅ 阶段 1 已落地（ADR-040）· 阶段 2 待做 |
| [023-progressive-tool-disclosure.md](./023-progressive-tool-disclosure.md) | 渐进式工具披露 — 工具降 name-only + `tool_search` 按需提升为原生（架构选 A=harness/Anthropic Tool Search，非 JVS 泛型代理 B；取代手动门控）。实测「一句 hi=17.9k 输入 token，工具 schema 占 73%」vs 对照组 JVS 11.3k（MCP 走 3 代理工具）+ 必要性证据链（Anthropic Tool Search 49%→74%、VS Code virtual tools、Cursor 40 上限、MCP 协议层不解决）+ 上游尽调（v1.3.13/anomalyco，零实现，#9461 open 无承诺 → 自建）+ 落点钉死（纯 plugin 不行，需窄接缝钩子 `experimental.chat.tools.transform`：plugin 类型加一行 + `prompt.ts:1460` fire 一次，策略全在 plugin；每 step 重解析 + tools 不进缓存=零惩罚）+ 委派工具不可 defer 硬约束（#3592） | 2026-06-26 | ✅ 已落地（ADR-036） |
| [029-dynamic-sidecar-ports.md](./029-dynamic-sidecar-ports.md) | Sidecar 端口动态化（dev 固定 / prod 动态）— 四 sidecar 端口 4096-4099 全为 Rust 编译期常量、dev/prod 同值；结论**可行**（上游 opencode 默认端口本就是 `0` 且回报真实端口，是我们传 `--port` 钉死的）；工作量在**五类消费者**如何得知端口（renderer 四处模块级常量 / localStorage 绝对地址 / gateway→opencode 硬编码 / opencode 孙进程 delegate-mcp 的 `ACP_CLIENT_PORT` 写进持久化 `opencode.json`〔实锤 stale〕/ 知识库 MCP 免疫但有 proxy 死代码地雷）；方案=`~/.ultrawork/run/ports.json` 运行时注册表 + 「先复用→dev 固定→prod bind(0)」三段式分配；**最大风险=固定端口今天充当事实上的单实例锁**，直接改会致 gateway 双开 IM 长连接顶号 + kb.db 双写 → 必须并入 `tauri-plugin-single-instance`；另发现现网即存在缺陷：端口被无关进程占用时我们会**杀掉它**（`lsof -ti :port` 匹配过宽，可能误杀仅碰巧持有该临时端口的进程）→ 建议先行单独修复；实证收敛：CSP=`null` 无阻塞 · gateway 无入站 webhook（四家全出站长连接）· 阶段④隐藏前置=三处裸 `EventSource` 不能带 Authorization 头，须先迁到 connector 的 fetch-reader | 2026-07-09 | ✅ 已拍板待开工（D1 优先固定+回退动态 · D2 single-instance · D3 apiBaseUrl 改 auto · D4 全做） |
| [028-im-channel-qr-and-brand-icons.md](./028-im-channel-qr-and-brand-icons.md) | 消息渠道扫码接入（钉钉/飞书/企微）+ 渠道品牌 icon — 三家官方设备流「扫码建机器人」机制源码级尽调（飞书 accounts registration/PersonalAgent+Addons · 钉钉 app/registration 三步 · 企微 ai/qc + 智能机器人长连接 101463）+ 收发链路可行性核实 + icon 素材/授权结论（**不用自画**：官方资源+开源库组合，Dify/LangBot 先例）+ 分期方案（icon→B0 拆分→B1 钉钉扫码化→B2 企微→B3 飞书）；对抗审查修订（QR 骨架必须改 gateway 后台轮询+缓存〔设备流 secret 一次性〕、secret 0600+masking、企微渠道 bot 严禁复用 CLI bot、真机实拍前置清单、D1-D6 已拍板） | 2026-07-07 | ✅ 已落地（ADR-044） |
| [027-office-cli-connectors.md](./027-office-cli-connectors.md) | 办公 CLI 连接器 — 钉钉 dws / 飞书 lark-cli / 企业微信 wecom-cli 接入方案：三家官方 Agent-native CLI（JSON 输出/dry-run/schema 自省/自带 Skills/凭证自管 Keychain）逐环节映射现有基建（依赖探针 ADR-040 / zip 技能管线 ADR-041 / rich PATH gotchas §6 / 内嵌 Node npm 安装），**零 vendor patch**；概念校正=CLI-first 非 MCP → 推荐「CLI 连接器」卡片（管检测/安装/鉴权/状态，不写 mcp 配置）而非 MCP wrapper；分期=飞书（`--no-wait` 授权最顺）→ 钉钉（白名单引导态）→ 企微（init 非交互待验+10 人分级）；与 Gateway 钉钉 channel（入站）正交 | 2026-07-06 | 调研记录 + 讨论中 |
| [026-byok-websearch.md](./026-byok-websearch.md) | BYOK 联网搜索 — Tavily + 阿里云 IQS 接入方案（websearch 复活）：根因=registry 门控（仅官方托管 provider）+ disclosure 折叠双层叠加致模型无搜索工具只能 webfetch 猜 URL；纠偏=百炼帮助页 `enable_search` 是模型参数非独立 API、独立 API=IQS「通晓」（key ≠ DashScope key）；拍板=路线 A（vendor patch 改造原生 websearch 为多 provider BYOK，key 借道 auth.json〔任意 id 已验安全〕，软刷新即时生效，无 key 自然降级）+ 单一工具 provider 优先级 + Exa 默认关 + qwen `enable_search` 连带做（**零 vendor 改动已验证**：模型级 options spread 进请求体；边界=search_info 来源被 AI SDK 丢弃）；设置页新增「工具」分区（integration 组，抄 BrowserServiceCard + models-section key 输入） | 2026-07-03 | ✅ 已拍板待开工 |
| [030-windows-webview2-runtime.md](./030-windows-webview2-runtime.md) | Windows WebView2 运行时依赖 — 现状=从未配置 `bundle.windows` 故吃 Tauri 默认 `downloadBootstrapper`；缺 runtime 时 `Section WebView2`(546) 排在 `Section Install`(638) 之前故**装不上而非白屏**（零文件落盘）；**源码级新发现**=NSIS 侧 `NSISdl.dll` 无任何 TLS 能力（导入表只有 WSOCK32，字符串 `GET %s HTTP/1.0`）且 `httpget.cpp:337` 忽略 scheme 一律连 :80 → 实测 fwlink 301 到 https 后仍走明文 80（CDN 照样返回真 PE `MZ`/1688792B）→ **安装期明文下载 + 无校验 + ExecWait 执行 = 用户级 RCE**（`installMode=CurrentUser` 压住了严重性）；两条反直觉结论=**(a) 装 runtime 不需要重启电脑**（微软文档只要求重启 app 进程，"要重启"来自 InstallShield 类框架）**(b) 国内下载源是伪命题**——`embedBootstrapper` 只省 1.6MB 那一跳、真正慢的 176MB 由微软 exe 内部拉取无法干预；且七大镜像站（阿里/腾讯/华为/USTC/TUNA/NJU/npmmirror）全无 WebView2，许可 2(b)(iii) 明令"必须直接从微软获取"否定第三方取包，而 2(a)(i) 明确允许随应用分发 → **正解=不下载、内置**；同类对照=用户点名四家（hermes/qoder/manus/cowork）无一 Tauri，最强反证是 Chatbox 因 WebView2 不稳弃 Tauri 迁回 Electron(PR#1428)，真正可抄的是 Clash Verge Rev/pot-desktop 的「主包 embed + 旁路 fixed 兜底」范式；体积不对称=我们 setup.exe 已 127MB（sidecar 撑的）故 +1.8MB 白送、+127MB 也只回到 DMG 量级；顺带发现 MSI 禁用理由（WiX light.exe 挂在 12k 文件资源树）**大概率已随 ADR-041 zip 化失效** | 2026-07-10 | ✅ 已实现 + CI 实证（ADR-046；D1=双包 embed 主包 + offline 变体 · D2=恢复 MSI · D3=最低 Win10 1803+，Windows 真机待验） |
| [031-right-sidebar-awareness-and-preview-layout.md](./031-right-sidebar-awareness-and-preview-layout.md) | 右侧栏感知缺失 + 产物预览挤压主会话 — 现状核验=① 右侧栏默认关且 `setRightOpen` **全仓无调用者**（无任何代码路径能程序化打开它）+ 产物 section 默认收起 + fs 扫描仅空闲时跑，三层遮挡叠加致产物/规划到达时用户**全程无感**；② 预览(`w-1/2`)+右侧栏(`w-72`)+chat(`w-1/2`) 三者并排 = **100%+288px 溢出**，因只有 chat 带 `min-w-0` 故全由它独吞 → 1440px 窗口下 chat 仅剩 ~300px、860px 正文列崩塌（ADR-009 当年就没算平这笔账，且文档已与代码漂移）；业界五条可迁移（VS Code Agents Window「手动操作压过自动行为」+ `explorer.autoReveal` 完整教训 · Canvas >10 行阈值 · Claude/Canvas 的 half→full 双态且 Claude 主动把默认从 full 降级成 half · Zed/Windsurf「列表≠详情」+ 反面教材 claude-code #42347「diff 抢主区」被 closed as not planned · NN/g passive→徽标 vs action-required→打断）；**一个不能照搬的先例**=Claude/Canvas 侧栏里没有产物列表，我们有 → 互斥会切断产物导航链；方案=布局状态机 `previewMode: closed\|half\|full` 收进 SidebarContext（三者永不并排，溢出账从结构上消失）+ 分级通知（规划首次自动展开一次 · 产物只打徽标 · 手动关闭永久压过自动 · kill switch）+ 产物派生提升到 Session 级（**代价：fs 扫描从「侧栏开着时每回合一次」变成「永远每回合一次」，须实测**）+ 预览内置产物导航 + full 态保留输入框；风险=`full` 态若用 `display:none` 会让 ADR-047 的 ResizeObserver 停摆 → 用零宽 + overflow hidden | 2026-07-11 | ✅ 已落地（ADR-048；D4 真机反馈后修订为「全屏无输入框、但行动项全保留」；三轮对抗审查修 13 项、真机验收全过） |
| [032-llm-idle-guard-tool-input-phase.md](./032-llm-idle-guard-tool-input-phase.md) | 回复中途 `LLM stream idle for 30000ms` — 根因=`idleGuard` 漏掉「工具参数生成」相位：`tool-input-start`→`tool-call` 之间 `inflightTools` 仍为空集、`sawFirstToken` 已置真 ⇒ 用最紧的 30s 杠去量该窗口；而 qwen/DashScope 在此相位有**缓冲模式**（报出函数名 + 吐几十字节前缀 → 服务端憋完整段参数 → 一次性 flush），实测静默 **37~65s，12/12 复现且 12/12 自行恢复**（直连 SSE 探针，绕开 AI SDK、不设 idle 超时）⇒ **纯误杀，provider 无问题**。两处外推被自己的实验推翻：① 「参数越大静默越久」被证伪——6~7 万字符参数反而全程流式（3900+ chunk、maxGap 仅 5~8s，0/3 停流）⇒ 存在**两种独立模式**，坏组合（缓冲+超大参数）的尾巴测不完 ⇒ 任何「实测最大值×N」的固定阈值都不安全；② 截图里 `0ms`+`Tool execution aborted` 是 `cleanup()` 的墓碑（重写 `time.start`），**工具从未执行**。取证陷阱：`processor.ts` 的 `tool-input-delta` 是空实现从不累积 `raw` ⇒ DB 的 `input={}` **无法区分「没收到参数」与「收到一半」**。`enable_search` 经 A/B 排除。拍板=工具参数相位给 **600s 兜底杠**（与 ACP `ACP_PROMPT_TOOL_SILENCE_MAX_MS` 对称；依据=时间上无法区分「憋参数」与「挂死」，而误杀合法 200s 参数生成 = 硬故障、真挂死多等几分钟 = 可恢复〔用户有停止按钮〕）；文本 30s / 首字 90s 不变 | 2026-07-11 | ✅ 根因已实证确认 · 方案已拍板待实施 |
| [035-clickable-links-and-inline-artifacts.md](./035-clickable-links-and-inline-artifacts.md) | 回复内链接可点 + 转录区内联产物 — **P0（链接，已落地）**：根因=markdown `<a>` 用 `target="_blank"`，Tauri WebView 直接吞掉 new-window 请求 ⇒ 点击零反应，而项目自己早把「必须用 `openUrl()`」写进 gotchas §151 / conventions §外部链接（设置页 7 处都照做了，**只有聊天正文违反自己的约定**）；顺带挖出两个更严重的既存 bug=**md 产物预览用默认 `<a>` ⇒ 点击在 webview 内原地导航、整个 app 被网页顶掉且无返回键** + 设置页/关于页 5 处裸 `<a>` 同样点不开；修法=共享 `MarkdownLink` + 协议白名单（**安全边界非洁癖**：转录区渲染的是半可信模型输出，可被抓取页面/IM 第三方消息带偏，而 `openUrl` 走系统 handler ⇒ 放行 `file:`/自定义 scheme = 用系统权限唤起任意本地 app；推论=**不要**为放行 IM deeplink 传宽松 `urlTransform`，那会拆掉 react-markdown 自带的 `defaultUrlTransform` 防线）+ 相对链接/锚点渲染成惰性文本 + **Rust `navigation_guard` fail-closed 兜底**（JS 侧走 openExternal 是约定，而约定会烂——转录区和产物预览都渲染模型写的内容，漏一个 `<a>` 就足以把 app 黑洞掉）。**P1/P2（产物，待落 ADR-052）**：业界调研=方向被 Claude/Gemini/Perplexity/Manus **全部**背书（只有 Devin 是「只在右栏」，而其用户是工程师、产物是代码仓库 ⇒ **我们目前的形态在业界是少数派**），最接近参照物=**Claude Artifacts**（「卡片=handoff 不是 workspace」+ half/full 状态机 + 侧栏专区 = 我们**已有全部地基**）；**但缩略图业界几乎无人做**（Claude 内联卡片用类型标签而非缩略图 · 查不到任何主流 AI chat 出 PDF 首页缩略图 · **Office/pptx 无一家做** ⇒ 直接挑战用户「要缩略图」的表述：真正解决「找不到」的是卡片本身，缩略图是无参照的增量，且 **pptx 恰是旗舰场景却技术上出不了图**）；噪音硬指标=**超过 4–5 个产物就刷爆对话**（vscode#261081 实测，全调研唯一带数字的证据）但**别折叠过头**（Cursor 默认 compact diff 被骂到官方承认「full should be default」——**我们「产物只在右侧栏」正是过度折叠、和 Cursor 同坑**）+ 别自动打开预览（Canvas 被骂最惨的点）。代码侧实证四条=① **时间窗归属不可复用**：窗口重叠是常态（end 取 `completed`、下窗 start 取下条 user `created`，用户边看流式边打字 ⇒ 实测重叠 **30s**，`TURN_GRACE_MS=5000` 只是零头；现有单测期望值自己断言的两个窗口就是重叠的）+ **窗口数 ≠ turn 数**（连发两条消息 ⇒ `groupIntoTurns` 出 1 个 turn 而 `sessionTurnWindows` 出 2 个窗，按索引配全错位 + 多出会认领文件的「幽灵窗」）+ `allMessages` vs 窗口化 `displayMessages` 错配 ⇒ **必须按 message id 对齐、绝不能按索引**；② **原「改 `extractArtifacts` 成 last-wins」方案被推翻**：徽标零风险（seen 是 `Set<path>` 结构上对顺序免疫，实测纯重排 unread=0）**但**会引入两个静默回归（`mime` 丢失 · `patch` 类型翻成 `file` ⇒ 带 diff 标的交付物掉进默认收起的工作文件组「消失」），而 **479 个测试一条都拦不住**（shim 跑全量套件 63 files/479 全绿 + canary 双跑证明 shim 生效）⇒ 正解=**`extractArtifacts` 一个字不改**、turn 归属另建派生表只在表上 last-wins（副产物：侧栏 first-wins 本就更好，last-wins 会让反复迭代的主交付物一直下沉）；③ ExecutionFlow 只渲染 `file`/`patch` part 的文件行 ⇒ 重复面很窄，建议改 ExecutionFlow 而非给卡片打补丁；④ `classifyArtifacts` 的「无 deliverable 就提升 working」是**集合属性非文件属性** ⇒ per-turn 下同一 `gen.py` 在不同 turn 被区别对待（与业界「无人做产物分级」独立撞上同一结论 ⇒ per-turn 不分组、只排序）。P2 技术约束=`mtimeMs` 已从 Rust 传上来只是被 `.map(h=>h.path)` 扔掉（保留它一石二鸟：turn 归属 + 缩略图缓存失效）· **缩略图槽位必须固定尺寸**（异步加载改历史 turn 高度，而 WebKit 无 scroll anchoring ⇒ 会把用户正在读的视口顶走）· 不缓存 objectURL 改缓存降采样小 dataURL（整类泄漏消失、内存有上限、图片与 PDF 同一条路）· pdf.js 单一共享 worker ⇒ lazy 是硬要求 | 2026-07-12 | P0 ✅ 已落地（`4a271866`）· P1 ✅ 已落地（`1c2c5413`，转录区产物卡片：归属表派生、SSOT 不动、幽灵窗丢弃、折叠阈值 4）· **P2 缩略图已砍**（卡片形态即业界标准，缩略图无人做；唯一待观察项＝图片卡片内嵌小图，因业界对图片是直接渲染内容而非给个图标）· 待落 ADR-052 |
| [037-windows-console-flash-on-exit.md](./037-windows-console-flash-on-exit.md) | Windows 退出时闪现多个 PowerShell 控制台窗口 — 根因（源码级确认，非推测）=release 是 GUI 子系统程序（`windows_subsystem = "windows"`，`main.rs:1`）**自身没有控制台**，此时派生控制台程序（`taskkill`/`netstat`/`tasklist`/`powershell`）若不带 `CREATE_NO_WINDOW` ⇒ Windows **为子进程新建可见控制台窗口**；`shutdown_sidecars()` 上四个 sidecar 各一次 `taskkill` + `kill_browser_mcp_processes()` 按 needle 循环起 **4 个 PowerShell**（且**无条件调用**——从没开过浏览器 MCP 也照弹）。**项目里早就有 `no_window()` 辅助函数、注释准确描述了该症状，却只覆盖 5/35 处调用点**（反向佐证：sidecar 走 `tauri-plugin-shell`，该插件源码 `process/mod.rs:173` 默认就加了这个 flag ⇒ 四个 sidecar 启停全程无窗 ⇒ 嫌疑干净收敛到「我们自己写的 `std::process::Command`」）。次要影响=`Get-CimInstance Win32_Process` 带 `CommandLine LIKE` 是 WMI 全进程表扫描，×4 串行 + PowerShell 冷启动 ⇒ 退出可观卡顿。方案 P0=**收敛而非逐个补**（35 处归一到唯一构造器 `sys_cmd()`）+ 源码级守卫（**CI 里没有任何东西能「看见」一个窗口闪过** ⇒ 源码是唯一守得住的层面）；P1=PowerShell **只枚举不杀进程**（对抗审查抓出两条：① 隐藏 PowerShell 会把窗口**转嫁**给它派生的 taskkill，Chrome helper 全命中 needle ⇒ 可能几十个窗口、**比原 bug 更糟**；② WQL needle **字面写在 powershell 自己的命令行里** ⇒ 它匹配到自身 ⇒ 自杀中断清理 ⇒ **Windows 浏览器清理很可能从来没可靠工作过**）；P2 刻意不做=按需跳过清理（门控判错=漏杀）/ `sysinfo` 取代 shell-out（独立重构）。残余=`npm install` 孙进程（同一转嫁规则，不劣化）· `Bun.spawn` 未传 `windowsHide`（**未确认**，症状应是「启动 ACP 会话时闪窗」而非退出时，不并入本次） | 2026-07-13 | ✅ 已实现 + **Windows 真机验收通过**（ADR-054 / v0.2.7；启动→退出零窗口 · 浏览器 MCP 后退出零窗口且 Chrome 清干净，**顺带坐实 PowerShell 自杀缺陷确实让清理夭折过**；「起 ACP 会话是否闪窗」无法验证——该机器未装 Claude/Gemini。详见 §7） |
| [047-third-party-oss-notices-and-commercial-compliance.md](./047-third-party-oss-notices-and-commercial-compliance.md) | 「设置-关于」第三方开源软件声明 + 商用合规基线 — 参考同类桌面云产品关于页（底部一排入口 → 独立整页表格：序号/名称+版本/许可协议/是否修改/网址）。**许可证实测盘点**：npm 626 唯一包（540 MIT，全树无 GPL/AGPL/SSPL/BUSL 阻断项）+ cargo 560 crate（Tauri 生态全permissive）+ 4 个 externalBin 二进制 + vendored 资源，**商用地基干净**。**结论=关于页加声明必要但不充分**：三大缺口＝(a) 捆绑二进制内嵌依赖未声明（opencode-server 是 bun 单文件内嵌数百 npm 包、`vendor/opencode` 无 NOTICE，手写表格必漏此层）(b) vendored pptxgenjs 许可头被 esbuild 剥离 (c) MIT/BSD/Apache 需保留**许可证全文**非仅列名；且 opencode 经 patch 改过 ⇒「是否修改=是」。商用还需 EULA/用户服务协议/隐私政策。**决策已拍板**＝独立子页整页表格 + 清单表+完整 NOTICES 全文（覆盖内嵌依赖）+ 三入口一并补。方案＝Phase1 构建期 `gen-notices.ts` 聚合四类来源产 `licenses.json`+`NOTICES.txt` 纳入 check-docs 漂移门禁 → Phase2 UI 二级视图（搜索+行内展开全文）+ 三入口 → Phase3 ADR/收尾。待法务提供三份协议正文 | 2026-07-20 | 🟡 方案已定稿 · 未动代码 |
| [046-sidebar-order-flaky-clock-and-negative-path-stderr.md](./046-sidebar-order-flaky-clock-and-negative-path-stderr.md) | 「sidebar-order.test.ts（config JSON parse）失败」溯源 — 实为**误读**（那行 `JSON Parse error` 来自 config.test.ts 的负路径用例 `returns DEFAULT_CONFIG on invalid JSON`，vitest 已归属正确文件、是读交错 stderr 时的人为误判；sidebar-order 6/6、全套 683/683 全绿）；顺挖两处：**隐患二（真 flaky）**=`groupSessionsByDate` 用真实挂钟算 `todayStart`，测试造 `updated=now-60_000` 断言 `today`，零点后 0–1 分钟窗口翻车（穷举全套中招面精确=1 条；`session-item` 相对文案不 flaky；**纯测试确定性、产品不动**；坑＝`now`/`todayStart` 模块加载期算，天真 `useFakeTimers` 会让源码/测试时钟 desync 更糟 ⇒ 必须**钉双时钟**）；**隐患一（非缺陷·洁净度）**=负路径 `console.error` 泄漏 stderr，局部 `spyOn` 静音 + 升级 `toHaveBeenCalledTimes(1)` 正向断言 | 2026-07-20 | ✅ 根因已实证 · 隐患二修复 + 隐患一降噪 |
| [038-startup-white-screen.md](./038-startup-white-screen.md) | 启动白屏（主界面渲染前的空窗口） — 根因（Tauri 源码级确认 + 实测，非推测）=`app.rs:2370-2383` 先按 config 建窗口（未设 `visible:false` ⇒ 默认可见）**再**调用户 setup 钩子，且整个 setup 跑在 `app.run()` **之前** ⇒ 事件循环未启动、主线程被 setup 占住 ⇒ WebView **一帧都画不出来**（白屏期窗口「僵住」不是渲染慢，是根本没在渲染）；主阻塞源=`start_sidecar("opencode-server")` 是四个 sidecar 里**唯一没丢进 `thread::spawn` 的**（gateway/knowledge/acp 都在后台线程且注释写着 "don't block UI"，代码注释自己承认 "OpenCode goes first (critical — blocks until ready)"），`await_sidecar_ready` 纯同步轮询 200ms/次、超时 15s×3；**实测 opencode spawn→`/global/health` 热启动 1295ms**（冷启动更久）。白屏=**四段串行**叠加：opencode 健康等待（主因）+ setup 文件重活（拷贝 124MB 二进制/解压技能 zip/`reap` 里的 `sleep(200ms)`，首装升级后显著）+ **2.4MB 单体 JS bundle**（无 code splitting、14 个 CodeMirror 语言包全在首屏 chunk）+ 前端两道门（`main.tsx:36` 的 `Promise.all` gate 未 resolve 前 `createRoot().render()` 根本不调用 · `root-layout.tsx:22` 的 `initializing` 空 div）；**关键洞察=第 1 段与第 3 段本可并行**（opencode 后台起、WebView 同时解析 JS）却被主线程阻塞逼成串行 ⇒ **解阻塞本身就是提速**（总时长从「相加」变「取大」）。**前置条件**=只要主线程还堵着，任何 splash（含 index.html 里的纯静态 HTML）都画不出来 ⇒「解阻塞」和「加 loading」不是两个可选项、是一件事的两半。方案 B（选定，vs A「UI 挂载即摘」）=splash 等 **UI 挂载 且 opencode ready**：多等 ~0.5s 换取**保住「首次渲染时后端必可用」不变量**、零业务组件改动、无半可用状态（A 会让会话列表等请求失败、改动面扩散）。改动=setup 只留 `install_signal_handlers` + 其余全移入**启动协调线程**（顺序零改动；关键细节=**选端口 `plan_port`〔几十 ms〕与等健康〔1.3s〕是两件事** ⇒ 先定端口再等健康 ⇒ `main.tsx` 启动门语义一行不用改）+ `index.html` 内联纯 HTML/CSS splash（零 JS 依赖，含内联主题探测读 `ultrawork-config`）+ Rust emit 真实阶段事件（**不做假百分比**）。**唯一新增风险（必须处理）**=setup 变非阻塞后「启动中途退出」从不可达变可达 ⇒ `shutdown_sidecars()` 用 `mem::take` 抽空注册表 + `remove_ports_json()` 后，协调线程仍会 `reg.push` 新 sidecar ⇒ **该进程永不被杀且下次 `reap_orphaned_sidecars` 也看不见它**（它只读 ports.json）⇒ 需 `SHUTTING_DOWN` 原子量 + spawn 前后双检查握手。顺带修复两个既存缺陷=**深色模式启动闪浅色**（`theme-context.tsx:16` `resolvedTheme` 初值硬编码 `"light"` 而默认 `theme:"system"`）+ **启动错误弹窗大概率从未成功弹出**（`tauri-plugin-dialog` 明写 `blocking_show` "should **NOT** be used when running on the main thread context"，而两处调用正在主线程 setup 里）。跨平台=**无平台分支、三平台同构**（Windows 侧 `sys_cmd()` 的 `CREATE_NO_WINDOW` 是**每个 Command** 的标志、与调用线程无关 ⇒ ADR-054 守卫在后台线程依然有效、不引入新闪窗）。e2e 硬约束=40+ Playwright 用例跑在普通浏览器上、`boot-progress` 事件永不到达 ⇒ 非 Tauri 环境必须「React 挂载即摘 splash」+ 超时逃生门，否则全挂 | 2026-07-13 | ✅ 已实现（ADR-055 / v0.2.8）· 实现后三路对抗审查抓出 9 条真缺陷全部已修 · **真机像素级验收**：白屏 4.23s→0.36s、应用可用 4.23s→2.22s |
