# 053 — deckcraft（做 PPT 技能）待优化清单

> 状态：📋 **登记中**（2026-07-23 建立）。本文件是 deckcraft 的**单一待办入口**，不是提案——每条只写「现象 / 证据 / 为什么还没做 / 动手时从哪切入」，成熟一条就升 ADR 或独立 discussions。
> 关联：ADR-061（deckcraft 为唯一默认做 PPT 技能）· ADR-066（密度双边带 + delivery_purpose）· ADR-067（对比度机器门禁）· **ADR-068（形式丰富度轴：风格 4→10 / 版式 10→20 / 骨相 token / 门禁 O10·O11·W3-5·E4 白名单·D6）** · discussions/043（自建 pptx 技能全景）· 051 / 052 / 054

## 一、结构性缺口：主观检查还没机器下界

ADR-066（密度轴，O9）和 ADR-067（对比度轴）都是同一套路：**把 `visual-review` 的主观 rubric 升级为机器门禁**。这条路只走了两格，rubric 里还有同构的几条。

| # | 项 | 现状 | 为什么值得做 | 切入点 |
|---|---|---|---|---|
| 1.1 | **R2 元素重叠** | 纯主观 | 与 R3/R4 完全同构：现有 probe 已在 Chrome 里拿到每个元素的 `getBoundingClientRect`，判矩形相交是现成数据。ADR-067 也明确「遮挡问题本门禁不回答」，正好由它补 | 扩 `probe_overflow.py`，用已采集的 rect 做两两相交（需排除父子/刻意叠层）；难点是「合法的紧贴」与「真重叠」的阈值，同样需先实测标定再定阈值 |
| 1.2 | **R5 对齐 / R8 一致性** | 纯主观 | 左缘/基线参差、跨页字号层级漂移都是可量化的（左缘 x 坐标聚类、字号取值集合跨页比对） | 同上，但优先级低于 1.1——错了不致命，且容易误杀刻意的错位设计 |
| 1.3 | **对比度门禁的召回只有合成用例背书** | ADR-067 已诚实标注 | 真机两轮跑下来模型都没产生对比度缺陷（该缺陷本身低频偶发），所以「该拦的拦得住」目前只由合成用例（1.10 浅底浅字 / 1.37 深底深字）证明 | 攒真实产物：以后每次真机跑 deck 顺手 `--dump-contrast` 存档，积累真实分布；不必专门造 |

## 二、提问轮行为（**需要先定产品语义，不是改代码**）

| # | 项 | 证据 | 卡在哪 |
|---|---|---|---|
| 2.1 | **第 2 轮（Phase 3 设计确认）会被整轮跳过** | 真机两轮实测：一次 `answered 3 questions (消费距离\|受众\|设计风格)`，一次 `answered 0 questions`——同技能同模型、prompt 具体程度相近，行为不同 ⇒ 模型自由裁量而非规则确定执行 | SKILL.md 第 1 轮结尾有「源材料能推断的不问」豁免，**第 2 轮没有**。但「两轮提问封顶」的「封顶」是**上限不是下限**，字面允许零轮。**要不要强制第 2 轮，是产品决定**：over-specified prompt 下不问是好体验还是漏掉设计确认？定了才好改 |
| 2.2 | **文档自相矛盾（可立即修）** | `gotchas §10⑪` 写「两轮 question 是 SKILL.md **硬门禁无降级**」，SKILL.md 写「两轮提问**封顶**」+「能推断的不问」 | 一处说必问、一处说可不问。改一句话的事，但改哪一句取决于 2.1 的结论，故一并处理 |
| 2.3 | `delivery_purpose` 询问策略偏保守 | 中性 prompt 产物落 `document` 而非缺省 `balanced`，即模型没问就自选（discussions/052 §七） | 与 2.1 同源，一起定 |

## 三、既有架构缺口（已知、已 defer）

| # | 项 | 影响 |
|---|---|---|
| 3.1 | **Team 委派下 question 门不可达** | `delegate.ts` 的 `onEvent` 只中继 `permission.asked/replied`、不中继 `question.asked/replied` → Team Leader 委派子会话跑 deckcraft 时两轮 question 被丢弃，阻塞到 600s 超时。单 agent 直连正常。**规避=Team 场景直连**。根治=delegate 对称中继 question + 前端支持回答委派会话 question。详见 gotchas §10⑪ |
| 3.2 | O3 断言检测器漏检「风险与挑战」一类 | 预存在缺口（ADR-066 的泛化 A/B 已证**未加剧**：14 条裸标签新增漏检 0）。属复合并列词组被误判为断言，低频、不致命 |

## 四、工程/运行时

| # | 项 | 证据 | 备注 |
|---|---|---|---|
| 4.1 | `probe_overflow.py` 每页 120s Chrome 超时偶发 | 实测定位=**连续大量启动**（测试套件一轮开 ~40 个实例）时的资源争用；单独探一份 deck 稳定 3/3，真实用法一次只探 7–14 页 | 已在测试脚手架加重试而非放宽产品超时。若将来真实场景也撞到，再考虑复用单个浏览器实例（当前是每页一进程） |
| 4.2 | 可编辑 pptx 的 **WPS / Windows / Linux 真机未验** | ADR-061 P2b 遗留 | 并入 Windows 真机验收批次 |
| 4.3 | 每页起一个 Chrome 进程 | 7–14 页即 7–14 次冷启动，是门禁链耗时大头（对比度测量本身只占 0.38%） | 复用单实例可显著提速，但要处理页面间状态污染；属性能优化，非正确性 |
| 4.4 | **ADR-068 的 Windows/Linux 真机欠账**（纯 skill 门禁性质，非桌面壳那批；**CI 只跑 TS+Rust、不覆盖 deckcraft python 门禁 ⇒ 只能真机**） | macOS 已过（真机测试 1/2：浅底 academic + 深底 tech-dark，9/9 维度不同、深底反转达标、门禁全绿） | 三条：**A（中·最该验）** probe `--window-size=1400` 在 Win/Linux 的实际视口——macOS 装饰固定吃 87px（720→633、1400→1313），Win/Linux 开销未测；**有视口自检兜底**（装不下 720 即 `exit 2` 报错、绝不静默出错）。**B（低·观感）** 新字体配对 serif/mono/humanist 回退（Win 退 Consolas/SimSun、Linux 需 `fonts-noto-cjk`、humanist〔Avenir/Optima〕基本 mac 专有）——字形变宽的溢出由客户机 probe 自愈、对比度不受字体影响、humanist 风格非 mac 视觉打折。**C（低·既有约束）** `pick_variants.py` 在 Windows `python`（非 `python3`）下（SKILL.md 已有跨平台启动器指引）。三条均**不致数据损坏/崩溃**（最坏=报错/观感打折/换命令名）。验法：同真机测试 1/2、换 Win/Linux 机器，重点看 probe 是否正常跑完（A）+ 中文/字体有无方块退化（B）。**并入 Windows 真机验收批次（同 4.2）** |

## 五、交给人的判断（机器不该替）

| # | 项 |
|---|---|
| 5.1 | **对比度 2.3–4.5 区间的松紧**：门禁只拦「几乎不可见」（<2.3），2.3–4.5 是「看得见但可能费劲」，刻意放行。需真机视觉判断是否该抬阈值——机器判不了 |
| 5.2 | 密度三档（presentation / balanced / document）的实际观感 —— 真机 A/B 已过，后续换题材时仍需抽查 |

## 六、已确认**不需要**做的（免得后人重复评估）

- **伪元素文字纳入对比度测量**：模板、四个样例、真机模型产物中 `::before/::after` **零使用**，该边界是理论性的。
- **图片/渐变底上的文字判负**：刻意不做——合成背景非真值，宁可漏报也不拿猜测拦人（ADR-067 D3）。
- **新增密集承载版式**：实测 S03/S06/S10 现有骨架都装得下远超旧 cap 的密集内容（ADR-066 D4）。
- **把对比度阈值抬到 WCAG AA（4.5）**：会与技能自身的 muted/accent 风格打架（实测合法值低至 2.57），门禁定位是「人眼还看得见吗」而非无障碍合规。
