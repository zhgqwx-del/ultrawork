{
  "name": "@agent/channel-gateway",
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun run --bun src/index.ts",
    "build": "bun build --compile src/index.ts --outfile dist/channel-gateway",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "echo 'No linting configured yet'"
  },
  "dependencies": {
    "@agent/api-client": "workspace:*",
    "dingtalk-stream": "^2.1.4",
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "typescript": "^5.8.2",
    "vitest": "^4.0.18"
  }
}
